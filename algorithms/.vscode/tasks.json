{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "0.1.0",
    "command": "g++",
    "isShellCommand": true,
    // include -g for compiling with debug information (otherwise debugging won't work')
    "args": ["-g", "${workspaceRoot}/main.cpp"],
    "showOutput": "always"
}